# 2026-06-20 Mini Regression

## Purpose

Run two small regression tests after recent Skill changes:

1. `visual-source-qc-mini`: verifies that source restoration uses model vision for table/figure QC.
2. `external-evidence-routing-mini`: verifies that full-evidence audit routes external evidence by type, instead of replacing scholarly or official routes with generic web search.

## Expected Outputs

Each branch should write its own `log.md`, outputs, and a short pass/fail note. The run root should contain `regression-comparison.md`.

