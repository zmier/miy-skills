# IDA 中恢复 ARM32 Thumb 函数

当运行时地址最低位为 `1` 时，先保存原始函数指针，再用清除最低位的地址定位代码。

IDA 自动分析若未创建函数：

1. 在目标地址设置 Thumb 状态 `T=1`；
2. 删除错误数据定义；
3. 创建指令；
4. 创建函数；
5. 等待自动分析后再调用 Hex-Rays。

新版 IDA 可能把短包装入口与远处主体合并为同一个函数。此时比较跳转、调用关系和伪代码语义，不以旧版 IDA 的函数切分方式作为唯一正确答案。
